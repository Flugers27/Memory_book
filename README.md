# Memory Book

Проект «Memory Book» — это веб-приложение для создания и управления персонализированными страницами памяти. Архитектура построена по принципу микросервисов, включает сервисы аутентификации, управления памятью, API-шлюз и современный React-фронтенд.

## 🚀 Технологический стек

### Бэкенд
- **Python 3.11+** с использованием **FastAPI** для создания API.
- **PostgreSQL** в качестве основной базы данных (SQLAlchemy 2.0).
- **JWT** для аутентификации и авторизации.
- **Argon2** для хеширования паролей.
- **SQLAlchemy** для ORM.

### Микросервисы
1. **Auth Service** (`services/Auth/`) — регистрация, вход, управление пользователями, JWT-токены.
2. **Memory Service** (`services/Memory/`) — создание, чтение, обновление и удаление страниц памяти, агентов.
3. **API Gateway** (`gateway/`) — единая точка входа, маршрутизация запросов, аутентификация, проксирование.

### Фронтенд
- **React 19** с TypeScript.
- **Vite** для сборки.
- **React Router** для навигации.
- **Axios** для HTTP-запросов.

### Инфраструктура
- Конфигурация через `.env` и конфигурационные классы.
- Логирование в файлы с помощью `structlog`.
- Готовые скрипты для запуска всех сервисов (`scripts/`).

## 📁 Структура проекта

```
Memory_book/
├── .env                    # Переменные окружения
├── config.py               # Базовый конфиг
├── requirements.txt        # Зависимости Python
├── README.md               # Этот файл
├── database/               # Модуль работы с БД
│   ├── engine.py           # Создание движка SQLAlchemy
│   ├── session.py          # Сессии БД
│   └── models/             # Модели SQLAlchemy
├── gateway/                # API-шлюз
│   ├── main.py             # Основное приложение Gateway
│   ├── proxy.py            # Логика проксирования
│   ├── middleware.py       # Промежуточное ПО
│   └── config.py           # Конфигурация Gateway
├── services/               # Микросервисы
│   ├── Auth/               # Сервис аутентификации
│   │   ├── main.py         # Точка входа Auth
│   │   ├── auth_logic.py   # Логика аутентификации
│   │   ├── crud.py         # CRUD для пользователей
│   │   ├── routers/        # Роутеры FastAPI
│   │   └── config.py       # Конфигурация Auth
│   └── Memory/             # Сервис памяти
│       ├── main.py         # Точка входа Memory
│       ├── crud.py         # CRUD для агентов и страниц
│       ├── routers/        # Роутеры
│       └── config.py       # Конфигурация Memory
├── shared/                 # Общие модули
│   ├── config.py           # Общие настройки
│   ├── jwt_utils.py        # Утилиты JWT
│   └── exceptions.py       # Общие исключения
├── memory-book-frontend/   # React-фронтенд
│   ├── src/
│   │   ├── pages/          # Страницы приложения
│   │   ├── components/     # React-компоненты
│   │   ├── api/            # API-клиенты
│   │   └── App.tsx         # Корневой компонент
│   └── package.json
└── scripts/                # Вспомогательные скрипты
    ├── start_all.py        # Запуск всех сервисов
    ├── health_check.py     # Проверка здоровья
    └── stop_all.py         # Остановка всех сервисов
```

## 🛠️ Установка и запуск

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd Memory_book
```

### 2. Настройка окружения
Создайте файл `.env` на основе `.env.example` (если есть) или скопируйте из примера:
```env
DEBUG=true
DATABASE_URL=postgresql://user:password@localhost/memory_book_db
SECRET_KEY=your-secret-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7
BCRYPT_ROUNDS=12
CORS_ORIGINS=http://localhost:3000,http://localhost:8000
AUTH_PORT=8001
MEMORY_PORT=8002
GATEWAY_PORT=8000
AUTH_SERVICE_URL=http://localhost:8001
MEMORY_SERVICE_URL=http://localhost:8002
```

### 3. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 4. Настройка базы данных
Убедитесь, что PostgreSQL запущен, и создайте базу данных:
```sql
CREATE DATABASE memory_book_db;
```
Применение миграций (если используются Alembic) или создание таблиц через SQLAlchemy (при первом запуске модели автоматически создадут таблицы).

### 5. Запуск сервисов
#### Вариант A: Запуск через скрипт
```bash
python scripts/start_all.py
```

#### Вариант B: Запуск вручную в отдельных терминалах
**Gateway:**
```bash
cd gateway
python main.py
```

**Auth Service:**
```bash
cd services/Auth
python main.py
```

**Memory Service:**
```bash
cd services/Memory
python main.py
```

### 6. Запуск фронтенда
```bash
cd memory-book-frontend
npm install
npm run dev
```

После этого приложение будет доступно:
- **Фронтенд:** http://localhost:3000
- **Gateway:** http://localhost:8000
- **Auth Service:** http://localhost:8001
- **Memory Service:** http://localhost:8002
- **Документация API (Swagger):** http://localhost:8000/docs

## 📡 API Endpoints

### Gateway (основные пути)
- `GET /` — информация о Gateway.
- `GET /health` — проверка здоровья Gateway.
- `GET /health/all` — проверка здоровья всех сервисов.
- `/{service}/{path}` — проксирование к соответствующему сервису.

### Auth Service
- `POST /auth/register` — регистрация пользователя.
- `POST /auth/login` — вход.
- `POST /auth/refresh` — обновление токена.
- `GET /auth/me` — информация о текущем пользователе.

### Memory Service
- `GET /memory/public` — список публичных страниц памяти.
- `GET /memory/page/{page_id}` — получение страницы.
- `POST /memory/page` — создание страницы (требуется аутентификация).
- `PUT /memory/page/{page_id}` — обновление страницы.
- `DELETE /memory/page/{page_id}` — удаление страницы.

Подробная документация доступна через Swagger UI по адресу `http://localhost:8000/docs`.

## 🧪 Тестирование

Запуск тестов:
```bash
pytest services/Auth/tests/
pytest services/Memory/tests/
```

Для запуска всех тестов с покрытием:
```bash
pytest --cov=services --cov-report=html
```

## 🔧 Конфигурация

Каждый сервис имеет свой конфигурационный файл (`config.py`), наследуемый от базового `config.py`. Настройки могут быть переопределены через переменные окружения.

Ключевые настройки:
- **Порты сервисов** (задаются в `.env`).
- **JWT Secret Key** — должен быть надёжным и уникальным.
- **CORS Origins** — список разрешённых доменов.
- **Логирование** — уровень логирования и путь к файлам логов.

## 🐛 Известные проблемы и улучшения

Список выявленных проблем и рекомендаций по улучшению:

1. **Конфигурационные ошибки** — в `main.py` сервисов используются несуществующие поля `HOST` и `PORT`. Необходимо заменить на `SERVICE_PORT` и добавить `HOST` в конфигурацию.
2. **Отсутствие зависимостей** — добавить `argon2-cffi` в `requirements.txt`.
3. **Дублирование зависимостей** — убрать дубликат `httpx` в `requirements.txt`.
4. **Пустой файл** `shared/database.py` — заполнить или удалить.
5. **Ошибки типов в CRUD** — проверить соответствие типов возвращаемых значений.
6. **Безопасность** — установить `SECRET_KEY` в `.env`, ограничить CORS.
7. **Логирование** — заменить `print` на `logger`.
8. **Тесты** — добавить unit- и интеграционные тесты.
9. **Документация** — расширить текущий README.

Подробности см. в отчёте анализа проекта.

## 📄 Лицензия

Проект распространяется под лицензией MIT. Подробности см. в файле LICENSE.

## 🤝 Участие в разработке

Если вы хотите внести вклад в проект:
1. Форкните репозиторий.
2. Создайте ветку для ваших изменений.
3. Внесите изменения и напишите тесты.
4. Отправьте pull request.

По вопросам и предложениям обращайтесь к автору проекта.

---
**Версия:** 1.0.0  
**Дата последнего обновления:** 2025-12-24  
