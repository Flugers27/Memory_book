# 📄 requirements.txt в корне проекта
# ============================================================================
# БАЗОВЫЕ ЗАВИСИМОСТИ ДЛЯ ВСЕХ СЕРВИСОВ
# ============================================================================

# FastAPI и ASGI сервер
fastapi==0.104.1
uvicorn[standard]==0.24.0

# HTTP клиент для Gateway
httpx==0.25.1

# JWT токены (нужен и Auth, и Gateway)
python-jose[cryptography]==3.3.0

# Работа с переменными окружения
python-dotenv==1.0.0

# База данных (если у всех сервисов общая БД)
sqlalchemy==2.0.23
psycopg2-binary==2.9.9  # PostgreSQL драйвер
# или для SQLite:
# sqlite3 (встроен в Python)

# Пароли и хеширование
passlib[bcrypt]==1.7.4

# Валидация данных
pydantic==2.5.0
pydantic-settings==2.1.0

# UUID
uuid==1.30

# Дата/время
python-dateutil==2.8.2

# Логирование
structlog==23.2.0

# ============================================================================
# ОПЦИОНАЛЬНЫЕ (можно вынести в requirements-dev.txt)
# ============================================================================

# Для разработки
python-multipart==0.0.6  # Загрузка файлов
watchfiles==0.21.0       # Автоматическая перезагрузка

# Для тестирования
pytest==7.4.3
pytest-asyncio==0.21.1